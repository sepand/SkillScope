# Versions are pinned exactly (not >=) and chosen to be at least 14 days old
# at time of pinning, per project policy against installing very-recently-published
# packages (supply-chain risk mitigation).
anthropic==1.2.0
pyyaml==6.0.3
rich==15.0.0
flask==3.1.3
python-dotenv==1.2.2
